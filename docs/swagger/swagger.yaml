basePath: /
definitions:
  models.ResultModel:
    properties:
      body:
        type: object
      code:
        type: integer
      msg:
        type: string
    type: object
host: '{{.Host}}'
info:
  contact: {}
  description: WebAPI v0.0.1
  license: {}
  title: WebAPI
  version: 0.0.1
paths:
  /Data/GetAccountInfo:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ResultModel'
            type: object
      summary: 获取账户信息
      tags:
      - 账户
  /Login:
    get:
      parameters:
      - description: userName
        in: query
        name: userName
        required: true
        type: string
      - description: userPwd
        in: query
        name: userPwd
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/models.ResultModel'
            type: object
      summary: 登录
      tags:
      - 账号
swagger: "2.0"
